import { Button, Typography } from "@mui/material";

const QuizSection = () => {
  return (
    <>
      <Button variant="contained">Create Quiz</Button>
      <Typography mt={2}>
        Quiz list will appear here
      </Typography>
    </>
  );
};

export default QuizSection;